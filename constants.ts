export const MIN_CRASH_TIME_MS = 1000;
export const MAX_CRASH_TIME_MS = 12000; // Increased to 12s to allow higher profits
export const RESET_DELAY_MS = 3000; // Longer reset to celebrate win
export const BASE_PROFIT = 10;
export const PROFIT_MULTIPLIER = 1.21; // Tuned to reach ~100 FCFA in about 12 seconds
export const SCALE_GROWTH_RATE = 0.15; // Slower growth to accommodate longer duration
export const INITIAL_SCALE = 1.0;